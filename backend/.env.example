DATABASE_URL="postgresql://crm:crm_local_password@localhost:5432/crm"
PORT=4000
ADMIN_USERNAME=admin
# Générer avec: node -e "console.log(require('bcryptjs').hashSync('votre-mot-de-passe', 10))"
ADMIN_PASSWORD_HASH=
# Générer avec: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=
# Laisser vide = mode simulé (aucun email réel envoyé, tout est journalisé en base).
RESEND_API_KEY=
MAIL_FROM=
